# stock-agent 工程修复说明（2026-08-27）

本批修改共涉及 63 个文件（净删除约 957 行），配套《stock-agent 工程维护分析报告.pdf》。
以下为核心变更索引，按"功能性修复 / 死代码清除 / 测试资产同步 / 代码卫生"四类组织。

## 一、功能性 Bug 修复

| 编号 | 位置 | 修复内容 |
|------|------|----------|
| B1(严重) | src/analyzers/timing_engine.py | 补导入 get_market_environment，修复 D1 恐慌抄底充分条件链路整体失效；删除重复的成交量分位判定块与多个死局部变量 |
| B2(高) | tests/test_stock_agent.py | 删除两个悬空的 @pytest.fixture，恢复测试套件可收集 |
| B3(中) | src/analyzers/lhb_scorer.py | 补 from ..utils.session_cache import get_session_cache，消除 NameError；清理迁移遗留死缓存全局变量 |
| B4(中) | src/analyzers/timing_engine.py | DEFAULT_TIMING_CONFIG.stop_loss 对齐 config/timing.yaml（0.95 / 0.06） |
| B5(低) | src/decision/aggregator.py | SignalSummary 注解解除对已废弃 sector_scanner 类型的耦合 |
| B6(低) | src/orchestrator/engine.py | 删除 _do_intraday 内遮蔽顶层的重复导入 |

## 二、死代码清除

- 物理删除整模块：
  - `src/analyzers/sector_scanner.py`（717 行；P1-17 已声明废弃，统一用 sector_ranker）
  - `src/utils/cache_registry.py`（零引用）
- 局部死代码：aggregator 任务⑤空转循环、stock_data 形态强度死计算链、
  sw_industry/institutional_scorer 无效 global 声明、market_mode_adaptive.above_ma50、
  backtest_engine.current_close、sector_map_service.percentile/today 等（详见报告第三章表 3-1）。
- 按保守策略保留并登记为「未接线功能」：src/analyzers/institutional_trapped.py（F2）、
  push/templates.render_pre_market_summary。

## 三、测试资产同步至现行契约

- timing fixture 补挂 DEFAULT_TIMING_CONFIG（匹配参数化重构后的 _cfg 取参路径）。
- UT-05b / UT-05c 更新为 C1-v3 后的止损契约（fallback 不再乘系数；候选仅均线族+布林下轨），
  浮点断言改 pytest.approx（实现层不做 round）。
- RT-01 / RT-02 标记 skip 并注明对应实现已在信号服务模式重构中移除；
  RT-04 断言改为「午后检查并入 _do_intraday」的新架构事实。
- 结果：46 passed + 2 skipped（修复前：pytest 收集即崩溃）。

## 四、代码卫生

- ruff 自动修复 F401/F541/F811 共 127 处；人工清理全部 F841 死赋值与 B007 循环变量。
- B905：zip 调用补 strict=False（保持原有截断语义）。
- 终态：ruff --select F,E9,B 全绿；pyflakes 仅余 s1/s2 中 2 处带 noqa 的 pyarrow 探针误报。

## 验证记录

1. AST + py_compile 全量语法双检通过。
2. python -m src.main init：全部 10 个 YAML 配置校验 OK，数据库初始化 OK。
3. pytest tests/：46 通过 / 2 文档化跳过。
4. mock 冒烟：score_lhb 缺数据时优雅降级；_check_panic_bottom 注入模拟市场环境后
   能正常产出恐慌抄底信号（证明 B1 修复使 D1 设计从纸面回到可用状态）。

## 待决事项（详见 PDF 报告第五章）

- institutional_trapped(F2) 与 render_pre_market_summary 的接线或退役决策。
- 复用收敛路线 R1-R9（ATR 抽公共函数、akshare 三轨入口归一、K 线键位适配器等），
  建议在回测基线护航下分批实施。
