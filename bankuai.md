### **一句话定位：给 stock-agent 补上"板块的眼睛"——让它从"只看个股和大盘"升级为"能看见板块间资金跷跷板"，整个 S 系列就是为这一件事服务的。v2 修订：数据源全面改同花顺（THS，本环境实测可用），概念降级为纯测量层，消费端按"行业版可接入 / 概念版默认测量态"分设预期**

### **背景：你现在在哪，为什么要动**

阶段一修好了测量（组合级回测、置信区间、熊市样本），阶段二修好了规则（B1/C2/C1/D1/F9，策略转成低频大赚，2024 段转正）。策略的个股层和环境层已经立住了。这时引入博主（爱在冰川）的看盘体系做对照，发现他的五条核心盘感里，三条工程已经融合（五日线锚定=B1、缩量毛刺不打断=C2、钱没来不抄底=D1），但有两条是**工程的结构性空白**：一是板块之间的虹吸与回流关系（科技涨、4000 家跌的跷跷板），二是"该涨不涨"的预期落空警报（7 月 1 日医药大涨、科技普跌就是退潮第一信号）。这两条恰好都指向同一个缺失的层：**板块层**。目前的 sector_scanner 只给单个板块做独立体检，看不见板块之间的关系。

**v2 关键修订（有实测依据，非拍脑袋）**：v1 设想以东财（EM）板块接口为数据源。但本环境实测东财被封（stock_zh_a_hist 等全部 RemoteDisconnected），而**同花顺（THS）板块接口全部可用且数据最新**——行业指数/行业一览/概念指数/概念列表/概念热点榜均已实测到 2026-08-14。因此 v2 全面转 THS，删掉 EM 相关一切；同时概念层因"个股→概念成分股接口本版 akshare 无 THS 实现（仅 EM 被封/富途）"而**天然锁死消费路径**，正好与"概念纯测量"的定位重合，消费端按行业/概念分设预期。

### **方案：三条新规则，各一句话**

S1 板块相对强弱——以自动锚点日为区间起点，滚动算各板块对大盘的超额收益，找出主线在哪；S2 虹吸度——主线板块成交额占全市场比重的升降，判断资金是在抽血还是在回流，虹吸中冻结对立面板块的进场、回落时放开抄底；S3 预期验证——模式判定为进攻但主线板块实际走弱、连续两日不符，强制降级一档，这是博主"预期落空即信号"的工程化。

三条全部**只加权、不出新信号、不直接触发买卖**。消费矩阵（v2 拆成两个入口）：

| 消费路径 | 数据依赖 | 本环境状态 | 消费出口 |
| --- | --- | --- | --- |
| S1 行业RS加权（个股信号） | 个股→行业映射（sector_ranker 已活）+ THS 行业指数（✓已验） | 可行 | C1-C4 全过后接 Step0 调度器排序键 |
| S2 行业虹吸（冻结/放开） | THS 行业一览聚合（✓已验）+ 同上映射 | 可行 | 同上，经调度器"期望<-0.5%主动放弃"通道 |
| S3 预期验证（环境层降级） | 模式判定 + 行业一览涨跌比（✓已验） | 可行，零个股映射需求 | market_mode_adaptive 强制降档 |
| S1/S2 概念粒度加权 | 个股→概念映射（本环境无 THS 成分源） | 锁死 | 永久测量态，出口=周报肉眼消费 |

```mermaid
flowchart LR
    subgraph 数据层·THS
        A1[每日快照<br/>行业一览1 + 概念指数30<br/>≈31次/日] --> A5[(sector_snapshots/<br/>parquet+trade_date)]
        A2[历史初始化<br/>90行业+30概念<br/>一次性~120次] --> A6[(ths_cache/)]
        G[DataFreshnessGuard<br/>最后日期==最近交易日<br/>否则剔除+告警] --> A5
    end
    subgraph 指标层·纯记录
        A5 --> B1[anchor_detector<br/>锚点日]
        A5 --> B2[sector_rs_tracker<br/>rs5/rs10/rs_anchor]
        A5 --> B3[suction_index<br/>虹吸度·行业真占比/概念子集口径]
        A6 --> B4[expectation_divergence<br/>S3 预期验证]
    end
    subgraph 关卡
        B1 & B2 & B3 & B4 --> C{C1a行业覆盖≥80%<br/>C1b概念热点命中≥80%<br/>C2换手≤30%<br/>C3行业版CI不重叠<br/>C4 2024段不倒退}
    end
    C -->|通过| D1[行业版 sector_multiplier<br/>接 Step0 排序键]
    C -->|不通过| E[仅保留测量层]
    B1 & B2 & B3 --> D2[概念版<br/>写死测量态→周报]
```

### **阶段0：接口验证（v2 已提前实测，结论直接落盘）**

实测日期 2026-08-15，akshare 1.18.64。已验证项（全部通过）：

| 接口 | 实测结果 | 用途 |
| --- | --- | --- |
| `stock_board_industry_index_ths("半导体")` | ✅ 最新 2026-08-14，历史完整 | S1 行业RS、锚点 |
| `stock_board_industry_summary_ths()` | ✅ 90 行业，含总成交额/上涨下跌家数/领涨股 | S2 行业虹吸、S3 涨跌比 |
| `stock_board_concept_index_ths(name, start_date, end_date)` | ✅ 6/6 通过，全量历史（成熟概念至 2020），含成交量+成交额，显式日期到 2026-08-14 | S1 概念RS、锚点、概念虹吸 |
| `stock_board_concept_index_ths(name)` 不带日期 | ⚠️ **静默截断在 2025-02-28** | 陷阱① |
| `stock_board_concept_name_ths()` | ✅ 375 个概念 | 概念宇宙 |
| `stock_board_concept_summary_ths()` | ✅ 但仅 50 热点榜（驱动事件/龙头股/成分数），**无成交额/涨跌家数** | 仅 C1b 概念近似真值集 |
| `stock_board_concept_cons_ths` | ❌ 本版 akshare 无此函数（成分股仅 EM 被封/富途） | 陷阱② |

**陷阱①静默截断（必须工程化为通用守卫）**：概念指数不带日期参数时，akshare 默认 end_date 只到 2025-02-28，数据"看起来正常只是旧"。这是数据管道最阴险的故障模式，且不止概念指数会有。**所有落盘数据强制携带 `trade_date` 字段，盘后统一断言"最后日期 == 最近交易日"，不满足则该数据源当日不进任何计算并推送告警**（DataFreshnessGuard，见阶段A）。合并点：`institutional_scorer.py:104 _find_recent_trading_day`（即 F9"跳过数据未更新日/周末"逻辑），泛化为单一 util。

**陷阱②概念成分股缺失（锁死概念消费的硬约束）**：个股→概念映射（v1 的 A3 倒排索引）本环境无 THS 数据源。概念版消费因此**物理不可行，不是设计放弃**——这个约束让"概念纯测量"成为数据源强制结果，反而干净。概念层的出口是周报供人肉眼消费，恰好复刻博主用法（人看概念 RS 排名做盘感，机器不碰）。

### **阶段A：数据层（THS，v2 修订）**

**A1 每日快照落盘**（盘后 2 次固定时点，如 16:00/17:00，非交易日自动跳过）：
- 行业一览 `stock_board_industry_summary_ths()` —— **1 次调用**，含当日涨跌幅/总成交额/涨跌家数/领涨股，快照日积月累即得行业层完整指标历史
- 概念指数 `stock_board_concept_index_ths(name, start_date=今日, end_date=今日)` —— **跟踪子集 ≤30 个概念**，每次取最新一行（收盘价+成交额），子集选择规则 = 博主流水板块（8 个，以实测名称为准）∪ 动态池当前龙头概念，写死进 config
- 所有快照 parquet 强制带 `trade_date` 字段，喂给 DataFreshnessGuard
- **调用量恒为 31 次/日**（行业 1 + 概念 30），非 v1 声称的 2 次；这是"概念虹吸需逐概念指数成交额聚合"的必然成本，如实登记

**A2 历史初始化（一次性，替代 v1 的申万回填）**：90 个 THS 行业指数 + 30 个跟踪概念指数，各拉一次全量历史（2020-01-01 起，成熟板块至 2020，新板块从成立日起，不足 120 交易日记 `history_limited`），落盘 `ths_cache/`。复用 v1 倒排那套**分批+断点续传**模式，约 120 次调用、一次完成。不建申万骨架（代码库 `sw_industry.py` 已是 THS 体系，v1 的申万回填方向与之相反，删除）。

**A3/A4 删除**：倒排索引（概念成分无源）与 alias.yaml 消费端（成分重叠校验无数据源）移除。概念→别名映射仅存一份"博主流水概念名对照表"（人工 30 分钟 + 龙头股核对），供周报标注用，不做 Jaccard 校验。

**DataFreshnessGuard（阶段A内嵌，通用守卫）** `src/loop/data_freshness.py`：(1) 所有落盘数据强制 `trade_date`；(2) 盘后断言"最后日期==最近交易日"；(3) 不满足 → 该源当日剔除计算 + pushplus 告警；(4) 与 F9 周末/未更新跳过逻辑合并为单一 util（泛化 `institutional_scorer._find_recent_trading_day`）；(5) 单测断言 `latest >= today-5` 作第二道网。目标：任何未来接口的静默截断（含 THS 改版）都被罩住。

### **阶段B：指标层（Week 1-5，与快照积累并行，纯记录）**

**B1 锚点判定** `anchor_detector.py`：自最新交易日向前扫描，返回最近满足任一条件的交易日：1) |单日涨跌幅|≥2.0%；2) 收盘创20日新高/新低；3) 成交量250日分位≥70%（与 D1 共用参数源）。锚点前移置 `anchor_shifted=True`，下游重算 `rs_anchor`。行业层用行业指数历史（✓已回填）；概念层用概念指数历史（✓已回填，新概念 `history_limited`）。

**B2 RS 计算器** `sector_rs_tracker.py`，双口径并存：`rs_n(t)` = 板块近n日累计收益 - 上证近n日累计收益，n∈{5,10}；`anchored_excess` = 锚点日收盘→当前的累计超额。bench 取上证指数（已有数据，零新增调用）。

**B3 虹吸度** `suction_index.py`：`suction_level = 板块成交额 / 分母`；`suction_state`：5日变化率 > +30% → 'siphoning'（冻结对立面进场）；< -20% → 'releasing'（D1 置信+1）；其余 'stable'。**口径坑①（登记）**：行业层分母=全市场成交额（一览真占比）；概念层分母=仅跟踪子集 30 个的成交额之和（**子集相对强度，非市场真占比**）。两套虹吸不同量纲，不可直接比较，概念虹吸仅用于周报展示与 C1b 近似覆盖，不进入消费。

**B4 S3 预期验证** `expectation_divergence.py`：`mode=='attack'` 且主线板块中位数涨幅 < 0 且 涨跌比 < 0.8 → 触发；`DivergenceCounter` 连续 2 日触发 → 输出 `force_downgrade`（attack→defend 降一档），次日盘前由 `market_mode_adaptive` 读取。零个股映射需求，行业一览已给涨跌比，Week 1 即可上线。

**B5 板块池聚合器** `sector_pool.py`：1) 四条入围规则筛 dynamic 层（快照历史不足 20 日自动降级，第1周仅开"成交额前60"，第2周加 5 日动量）；2) 合并 anchor 层（portfolio concept_tags ∪ 周报概念名）；3) 调 B1/B2/B3 算指标；4) mainline 判定：rs_10 前3 ∩ 虹吸前3 ∩ stage∈{lead,confirm}；5) 落盘 `sector_pool.json`。

### **阶段C：关卡检验（快照积累满 4~6 周后）**

| 关卡 | 脚本 | 通过标准 | 不通过动作 |
| --- | --- | --- | --- |
| C1a 行业覆盖 | `s1_gate_coverage.py` | 行业动态池对次日涨幅前10行业覆盖率 ≥ 80%（用一览快照攒次日真值，**保留预测语义**） | 放宽入围阈值，延长纯记录期 |
| C1b 概念覆盖 | 同上 | 跟踪子集30 对当日50热点榜命中率 ≥ 80%（**近似口径，见下**） | 同上 |
| C2 稳定性 | `s1_gate_stability.py` | 池子日换手率 ≤ 30% | 收紧动量规则，检查锚点敏感度 |
| C3 有效性 | `s1_gate_effectiveness.py` | **行业版**加权 vs 不加权的信号期望，cluster-robust 95% CI 不重叠（复用按日聚类 bootstrap） | 板块层永久停留在测量态（消费端预期=默认结局） |
| C4 回归 | 十交易日永久回归测试集 | 全部通过，2024 段期望 ≥ +2.10% 不倒退 | 回滚消费端 patch |
| C5 实盘影子 | 1~3 个月 pushplus 推送记录 | 快照/守卫零静默失败 | 修补数据层再重计时 |

**C1b 口径声明（语义降级必须写死）**：概念宇宙 375 个的"次日涨幅榜"本身要 375 次调用才知道（检验比建设还贵），故用 `concept_summary_ths` 的 50 热点榜做近似真值集。**热点榜≠涨幅榜**（THS 当日新闻驱动选择），所以 C1b 只能证明"在跟踪当日热点"，证明不了"预判能力"——它降级为跟踪自检，唯一保留预测语义的是 C1a 行业覆盖。快照时点固定为盘后 2 次（热点榜盘中变化，时点钉死防串）。

### **阶段D：消费接入（C1a/C2/C3/C4 通过后）**

`sector_multiplier` 拆两个入口：

```python
# timing_engine.py 行业版（经关卡后启用）
def sector_multiplier_industry(stock_code: str, pool: dict) -> float:
    ind = pool["stock_industry"].get(stock_code)      # sector_ranker 现有映射，已活
    if ind in pool["mainline_industry_top3"]:
        return cfg.w_mainline                          # 1.20，初值待回测标定
    if ind in pool["industry_siphoned"]:               # 主线行业虹吸时的对立面集合
        return 0.0                                     # 复用 Step0"期望<-0.5%主动放弃"通道
    return 1.0

# 概念版：写死测量态，不实现消费函数（数据源锁死，物理不可行）
# sector_multiplier_concept —— 仅周报展示概念 RS 排名，机器不消费
```

**洞②（登记）**：S2 行业版"对立面"集合（虹吸时冻结谁）在行业粒度没有博主那种概念成对语义，需要一个显式规则（相关性聚类 / 涨跌配对 / 与主线负相关的行业集合），初值待回测标定；若无合理解则 S2 行业版退化为"主线虹吸时全场冻结"，牺牲跷跷板语义，此降级选项也在 C3 检验范围内。

S3 的消费在 `market_mode_adaptive.py`：盘前读取 `DivergenceCounter`，连续触发达阈值则强制降级一档，并在推导链标注"S3 预期落空降级"供审计。

### **配置与治理登记**

`config/sector_pool.yaml` 收纳全部阈值（入围四规则、锚点三条件、虹吸两阈值、S3 三参数、加权乘数、最短历史门槛 120 交易日、跟踪子集名单），每参数在 GOVERNANCE.md 推断参数登记册新增并标注"初值待回测标定"。已知偏差登记：① 概念虹吸为子集相对强度（非市场占比），与行业虹吸不同量纲；② 概念层无成分股映射，覆盖检验用 50 热点榜近似；③ 概念成分漂移造成历史回填前视偏差（仅影响周报口径标注）；④ DataFreshnessGuard 断言规则与告警通道注册。

时间线：Day 1 阶段0已验证 ✓ → Week 1 阶段A快照上线（含守卫）+ B4 → Week 2-5 快照积累 + B1/B2/B3/B5 → Week 6 关卡检验 → 通过后阶段D行业版最小 patch + 全量回归。日稳态 31 次调用、初始化约 120 次一次性。任何一步失败的最大代价都是一个纯测量模块（概念层）+ 一个可回滚的行业版权重 patch，策略主线与阶段二成果零风险。
